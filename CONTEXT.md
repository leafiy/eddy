# Eddy

macOS 图片压缩 app:文件拖入即原地优化,所有编码引擎编译进 app 本体,零外部进程、零 GPL 依赖。

## Language

**动画资产(Animated Asset)**:
帧数大于 1 的图像文件(动画 GIF 等)。是不可分割的整体——任何处理必须完整保留帧数、时序与循环行为,任何只取首帧的操作都不得作用于它。
_Avoid_: 动图文件、多帧图片

**时序(Timing)**:
动画资产的播放行为:逐帧延迟(delay)加循环次数(loop count)。压缩在任何设置下都不得改变时序。
_Avoid_: 帧率(GIF 无全局帧率,只有逐帧延迟)

**Keep-if-smaller**:
只有当重编码结果严格小于原文件时才替换原件;"未变化"是合法结果,不是错误。
_Avoid_: 强制覆盖、压缩失败

**转换豁免(Conversion Exemption)**:
用户选定输出格式(PNG/JPEG)时,动画资产不参与转换,仍按原生格式原地优化;仅静态单帧文件参与转换。
_Avoid_: 拍扁、取首帧转换

**有损吸附(Lossy Snapping)**:
质量滑杆控制的误差容忍:像素值在阈值内吸附到上一帧或邻近值,以换取更小体积。只作用于像素颜色,永不触碰时序。
_Avoid_: 有损压缩(过于宽泛)

**尺寸压缩(Resize)**:
用户选择的最大宽度约束:超宽则等比缩小,永不放大。对动画资产作用于整个画布,所有帧统一缩放。
_Avoid_: 裁剪、缩略图

**一步操作(One-Step Operation)**:
Eddy 的交互哲学:每个动作原地作用于文件本身,没有中间态、向导或另存为。压缩、裁剪、还原都必须遵守。
_Avoid_: 多步撤销栈、编辑会话

**破坏性操作(Destructive Operation)**:
任何原地覆盖文件的动作——压缩替换或裁剪保存。同一文件的所有破坏性操作共享同一份初始原件。
_Avoid_: 编辑、修改

**初始原件(Intake Original)**:
用户交给 Eddy 的文件在第一次破坏性操作前的精确字节。还原的唯一目标。
_Avoid_: 临时源文件、源文件副本

**备份(Backup)**:
Eddy 为初始原件保存的副本,用于还原。压缩产生的备份与其历史条目同生命周期;仅裁剪产生的备份只存活到 app 下次启动。
_Avoid_: 缓存、快照

**还原(Restore)**:
用初始原件覆盖文件当前字节——含重建原格式、删除转换产物。还原会删除该文件的历史条目与备份,并移除主窗口行。无论中间发生过多少次操作,一次还原总是回到初始原件,没有分步还原。
_Avoid_: 撤销、undo、revert

**裁剪(Crop)**:
单一选框的画布变换:同一矩形可向内收缩(裁掉边缘)也可向外扩展(加衬底),每条边独立。直接以原格式覆盖保存;动画资产逐帧同区域处理,时序完整保留。
_Avoid_: 剪切、缩放

**扩展底色(Extension Background)**:
选框超出原像素的区域的填充色:白、黑、透明或自定义颜色。透明仅对支持 alpha 的格式开放。
_Avoid_: 底色(单用)、padding 色

**装饰(Decoration)**:
两个独立开关、固定轻微样式、烘焙进保存像素:贴图片边缘的细描边,和图片下方的柔和投影。均不可调参。
_Avoid_: 边框样式设置、滤镜
