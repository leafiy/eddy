# 03 — 二遍流式 + 全局调色板 + Bayer 抖动

**What to build:** 第一遍逐帧抽样累积全局直方图并推导全局调色板(中位切分,颜色数 = 滑杆 × 256,
下限 16);第二遍流式编码,常驻内存 ≤ 2-3 帧画布,与总帧数无关(死路 D 封死)。量化映射使用
Bayer 有序抖动(帧间稳定,不闪烁);颜色本就少的 GIF 走精确映射不抖动。

**Blocked by:** 02

**Status:** implemented — awaiting `swift test` verification on macOS

- [ ] 数百帧样本可处理,全部不变量保持
- [ ] 滑杆改变调色板颜色数(低档位输出调色板更小)
- [ ] 任何阶段不持有全帧位图数组
