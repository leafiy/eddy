# 04 — 帧间差分 + 透明像素复用

**What to build:** 未变化区域以透明索引复用表达,仅编码逐帧变化矩形;像素"由不透明变透明"的
帧自动将前一帧处置方式改为恢复背景并整帧重写,保证真透明语义不被差分吞掉。静背景 + 小运动区域
的典型动图体积显著下降。

**Blocked by:** 03

**Status:** implemented — awaiting `swift test` verification on macOS

- [ ] 静背景样本压缩后显著小于逐帧全幅写出
- [ ] 透明区域收缩/扩张的样本解码回放逐帧像素正确
- [ ] 全部时序不变量继续绿灯
